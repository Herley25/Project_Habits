import {
    StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // flex: 1,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
  button: {
    flexDirection: "row",
    height: 44,
    fontSize: 16,
    borderColor: "#8B5CF6",
    borderWidth: 1,
    padding: 8,
    alignItems: "center",
    borderRadius: 6,
  },
  text: {
    color: "#FFFFFF",
    marginLeft: 3,
    fontSize: 16,
  },
});
